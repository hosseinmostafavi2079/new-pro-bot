محتوای زیر را کپی کنید و در یک فایل جدید به نام **`README.md`** (در کنار فایل `manage.py` یا در پوشه اصلی پروژه) ذخیره کنید.

این فایل شامل **نصب**، **ساختار دیتابیس**، **تغییرات پورتی که دادیم** و **لیست APIها** است:

```markdown
# مستندات جامع سامانه مدیریت رویداد (Event Management System)

این پروژه یک سامانه ماژولار برای مدیریت رویدادها، فروش بلیط و حضور و غیاب است.
تکنولوژی‌های استفاده شده:
- **Backend:** Django Rest Framework (Python)
- **Database:** PostgreSQL (Dockerized)
- **Frontend:** React (Next.js) - *[در دست احداث]*

---

## 🚀 بخش اول: راهنمای نصب و راه‌اندازی سریع

اگر پروژه را روی سیستم جدیدی برده‌اید یا ویندوز عوض کرده‌اید، طبق مراحل زیر پیش بروید.

### ۱. پیش‌نیازها
- Python (نسخه ۳.۱۰ به بالا)
- Docker Desktop (حتماً روشن باشد)
- Git

### ۲. راه‌اندازی دیتابیس (Docker)
ما برای جلوگیری از تداخل با پورت‌های ویندوز، دیتابیس را روی پورت **54321** تنظیم کرده‌ایم.

1. ترمینال را در پوشه `backend` باز کنید.
2. دستور زیر را برای روشن کردن دیتابیس بزنید:
   ```bash
   docker-compose up -d

```

3. مطمئن شوید کانتینر روشن است:
```bash
docker ps

```

*(باید پورتی شبیه به `0.0.0.0:54321->5432/tcp` ببینید)*

### ۳. راه‌اندازی جنگو (Backend)

1. **نصب پکیج‌ها:**
```bash
pip install -r requirements.txt

```

2. **اعمال تغییرات دیتابیس:**
```bash
python manage.py migrate

```

3. **ساخت اکانت مدیریت (Admin):**
```bash
python manage.py createsuperuser

```

4. **اجرای سرور:**
```bash
python manage.py runserver

```

پروژه در آدرس `http://127.0.0.1:8000` در دسترس است.

---

## 🏗 بخش دوم: معماری سیستم و ماژول‌ها

این سیستم شامل ۳ اپلیکیشن (App) اصلی در جنگو است:

### 1. ماژول هسته (Core)

* **مسئولیت:** تنظیمات سراسری و داینامیک سایت.
* **جدول:** `SystemSetting`
* **کاربرد:** تغییر عنوان سایت، لوگو، کلیدهای API پیامک و درگاه پرداخت بدون نیاز به کدنویسی.

### 2. ماژول رویدادها (Events)

* **مسئولیت:** مدیریت محتوا و فروش.
* **جدول‌ها:**
* `Event`: اطلاعات رویداد (ظرفیت، قیمت، استاد).
* `Lecturer`: پروفایل اساتید.
* `Ticket`: بلیط‌های صادر شده (دارای کد یکتا UUID برای QR Code).



### 3. ماژول مالی (Payments)

* **مسئولیت:** ثبت وقایع مالی.
* **جدول:** `Transaction` (تاریخچه تراکنش‌های موفق و ناموفق کاربران).

---

## 🔌 بخش سوم: مستندات API (برای فرانت‌‌اند)

تیم فرانت‌‌اند باید از آدرس‌های زیر استفاده کند. تمام خروجی‌ها به صورت **JSON** است.

| عنوان | متد | آدرس (Endpoint) | توضیحات |
| --- | --- | --- | --- |
| **لیست رویدادها** | `GET` | `/api/events/` | دریافت تمام رویدادهای فعال برای صفحه اصلی |
| **جزئیات رویداد** | `GET` | `/api/events/{id}/` | دریافت اطلاعات کامل یک رویداد خاص |
| **لیست اساتید** | `GET` | `/api/lecturers/` | لیست مدرسین |
| **پنل ادمین** | - | `/admin/` | ورود به پنل مدیریت محتوا |

---

## 🛠 بخش چهارم: رفع اشکال و نکات مهم

### مشکل اتصال به دیتابیس (Connection Refused)

اگر خطای اتصال گرفتید، فایل `settings.py` را چک کنید.

* مقدار `HOST` باید حتماً **`127.0.0.1`** باشد (نه localhost).
* مقدار `PORT` باید حتماً **`54321`** باشد.

### ریست کردن داکر

اگر دیتابیس قفل کرد یا خراب شد:

```bash
docker-compose down
docker-compose up -d

```

### تغییر در مدل‌ها

اگر فیلدی را در فایل‌های `models.py` تغییر دادید، حتماً این دو دستور را بزنید:

```bash
python manage.py makemigrations
python manage.py migrate

